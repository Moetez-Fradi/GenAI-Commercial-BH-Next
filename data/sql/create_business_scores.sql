CREATE TABLE business_scores (
    REF_PERSONNE INT PRIMARY KEY,
    RAISON_SOCIALE VARCHAR(255),
    MATRICULE_FISCALE text,
    LIB_SECTEUR_ACTIVITE text,
    LIB_ACTIVITE text,
    VILLE text,
    LIB_GOUVERNORAT text,
    VILLE_GOUVERNORAT text,
    SECTEUR_GROUP text,
    ACTIVITE_GROUP text,
    RISK_PROFILE text,
    total_contracts FLOAT,
    active_contracts FLOAT,
    product_variety FLOAT,
    branch_variety FLOAT,
    total_premiums_paid DECIMAL(30, 20),
    avg_premium_per_contract DECIMAL(30, 20),
    total_capital_assured DECIMAL(30, 20),
    paid_ratio FLOAT,
    total_paid_contracts FLOAT,
    canceled_contracts FLOAT,
    loyalty_score DECIMAL(30, 20),
    financial_score DECIMAL(30, 20),
    payment_score DECIMAL(30, 20),
    final_client_score DECIMAL(30, 20),
    size_adjustment DECIMAL(30, 20),
    risk_adjustment DECIMAL(30, 20),
    client_segment text,
    risk_profile1 text,
    client_type text
);