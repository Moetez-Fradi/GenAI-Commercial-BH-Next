CREATE TABLE individual_scores (
    REF_PERSONNE INT PRIMARY KEY,
    total_contracts FLOAT,
    active_contracts FLOAT,
    product_variety FLOAT,
    branch_variety FLOAT,
    total_premiums_paid DECIMAL(30, 20),
    avg_premium_per_contract DECIMAL(30, 20),
    max_premium DECIMAL(30, 20),
    total_capital_assured DECIMAL(30, 20),
    avg_capital_per_contract DECIMAL(30, 20),
    paid_ratio FLOAT,
    total_paid_contracts FLOAT,
    total_unpaid_contracts FLOAT,
    expired_contracts FLOAT,
    canceled_contracts FLOAT,
    active_ratio FLOAT,
    premium_per_contract_ratio DECIMAL(30, 20),
    capital_premium_ratio DECIMAL(30, 20),
    product_density FLOAT,
    loyalty_score DECIMAL(30, 20),
    financial_score FLOAT,
    payment_score FLOAT,
    NOM_PRENOM VARCHAR(255),
    DATE_NAISSANCE DATETIME,
    LIEU_NAISSANCE VARCHAR(100),
    CODE_SEXE CHAR(1),
    SITUATION_FAMILIALE VARCHAR(50),
    NUM_PIECE_IDENTITE VARCHAR(50),
    LIB_SECTEUR_ACTIVITE VARCHAR(255),
    LIB_PROFESSION VARCHAR(255),
    VILLE VARCHAR(100),
    LIB_GOUVERNORAT VARCHAR(100),
    VILLE_GOUVERNORAT VARCHAR(150),
    AGE INT,
    id_length INT,
    SECTEUR_ACTIVITE_GROUP VARCHAR(100),
    PROFESSION_GROUP VARCHAR(100),
    final_client_score DECIMAL(30, 20),
    client_segment VARCHAR(50),
    risk_profile VARCHAR(50),
    client_type VARCHAR(50)
);